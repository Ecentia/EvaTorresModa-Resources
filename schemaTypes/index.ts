import category from './category'
import featuredCategories from './featuredCategories'
import product from './product'
import section from './section' // Importamos la nueva entidad

export const schemaTypes = [
  category,
  product,
  section, 
  featuredCategories
]